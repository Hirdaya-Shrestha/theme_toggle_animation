name: theme_toggle_animation
description: Smooth animated theme switching for Flutter with circle, line, and custom GIF mask effects. Zero dependencies.
version: 1.0.2
homepage: https://github.com/Hirdaya-Shrestha/theme_toggle_animation
repository: https://github.com/Hirdaya-Shrestha/theme_toggle_animation
issue_tracker: https://github.com/Hirdaya-Shrestha/theme_toggle_animation/issues
topics:
  - theme
  - animation
  - dark-mode
  - ui
  - widget

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
